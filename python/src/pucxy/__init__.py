"""pucxy — host-side proxy for the puck coding agent on Project Oberon 2013.

Bridges Oberon's serial line (minimal PUT/GET/CALL wire protocol) to an LLM
(rich, named agent tools). See ../../spec.md.
"""

__version__ = "0.0.1"
